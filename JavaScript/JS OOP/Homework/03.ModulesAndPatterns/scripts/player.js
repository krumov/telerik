var Player = (function () {
    "use strict";

    var Player = function (playerName, playerScore) {
        this.name = playerName;
        this.score = playerScore;
    };

    return Player;
})();

